# small numpy implementation of Pong for programmatic interaction

import numpy as np

class PongState(object):

	

	def __init__():
		self.screen_buffer = np.zeros((32,32))


	def draw():
		self.screen_buffer = np.zeros((32,32))

		self.draw_scores(self.screen_buffer)
		self.draw_paddles(self.screen_buffer)
		self.draw_ball(self.screen_buffer)
		
		return self.screen_buffer

	def update():
		pass


